version: "3.7"
services:
    {{cookiecutter.service_name}}:
        build:
            context: ..
            dockerfile: .devcontainer/Dockerfile.dev
        volumes:
            - ..:/usr/src/app
        command: sleep infinity